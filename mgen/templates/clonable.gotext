func (entity *_{{.Name}}) Clone() store.Object {
	ret := {{ .Name }}Factory()
	jsn, err := json.Marshal(*entity)
	if err != nil {
		log.Panic(err)
	}
	err = json.Unmarshal(jsn, &ret)
	if err != nil {
		log.Panic(err)
	}
	return ret
}
